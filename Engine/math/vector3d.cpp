#include "vector3d.h"

namespace engine
{
    namespace math
    {
        vector3d::vector3d() : x(0), y(0), z(0)
        {
            
        }
        
        vector3d::vector3d(double x1, double y1, double z1) : x(x1), y(y1), z(z1)
        {
            
        }
    }
}
